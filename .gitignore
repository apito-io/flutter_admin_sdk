.DS_Store
.dart_tool/
.packages
build/
*.iml
.idea/

# Local credentials (copy from secrets.env.example)
example/secrets.env
example/lib/apito_secrets.dart

example/apito_introspection.json

# env files tracked for single-dev monorepo setup (do not re-ignore .env / .env.local)
.env.*.local
